{
  "name": "fitcoachlanding",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:pages": "node scripts/generate-localized-pages.mjs",
    "dev": "vite",
    "build": "npm run generate:pages && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  }
}
